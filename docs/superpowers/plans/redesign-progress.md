# Design Redesign — Progress Ledger

Living dashboard for the dark-first redesign migration. This is the **compaction resume-anchor**: a freshly-compacted context reads the spec + this file to know where things stand. Git tags are the objective truth; this is the readable narrative.

- **Spec (roadmap):** `docs/superpowers/specs/2026-05-28-design-redesign-migration-design.md`
- **Design bundle (reference):** `docs/design/weekly-eats-redesign/`
- **Branch:** `claude-design-redesign` · **Beta:** `https://beta.weekly-eats.zamilyfam.com` (infra set up in Chunk 1 — Vercel domain bound to branch, DNS live, OAuth + env wired)
- **Draft PR:** [#89](https://github.com/zwrose/weekly-eats/pull/89) (long-lived, opened Chunk 1)
- **Audit backlog:** `docs/debt-audit-2026-05-28.md` _(created in Chunk 0)_

## Chunk status

| #   | Chunk                        | Status  | Tag               | Plan doc                             | PR test comment | Done       |
| --- | ---------------------------- | ------- | ----------------- | ------------------------------------ | --------------- | ---------- |
| 0   | Test baseline + hardening    | done    | redesign-chunk-00 | §6 of spec                           | n/a (no UI)     | 2026-05-28 |
| 1   | Foundation                   | done    | redesign-chunk-01 | redesign-chunk-01-foundation-plan.md | PR #89 comment  | 2026-05-28 |
| 2   | Nav chrome                   | done    | redesign-chunk-02 | redesign-chunk-02-nav-plan.md        | PR #89 comment  | 2026-05-28 |
| 3   | Meal Plans                   | pending | —                 | — _(review-plan)_                    | —               | —          |
| 4   | Recipes                      | pending | —                 | — _(review-plan)_                    | —               | —          |
| 5   | Shopping Lists               | pending | —                 | — _(review-plan)_                    | —               | —          |
| 6   | Pantry                       | pending | —                 | —                                    | —               | —          |
| 7   | Food Items                   | pending | —                 | —                                    | —               | —          |
| 8   | User Mgmt & Pending Approval | pending | —                 | —                                    | —               | —          |
| 9   | Settings (placeholder)       | pending | —                 | —                                    | —               | —          |
| 10  | Marketing / home             | pending | —                 | —                                    | —               | —          |
| 11  | Cleanup                      | pending | —                 | —                                    | —               | —          |

Status values: `pending` → `in-progress` → `done`. Per-chunk plans live at `docs/superpowers/plans/redesign-chunk-NN-<surface>-plan.md`.

## Next up

**Chunk 3 — Meal Plans (spec §4, B3 edit-flow decisions).** Largest chunk. `src/app/meal-plans/*`, `MealEditor`, `IngredientGroup`, `IngredientInput`, `MealPlanBrowser`, `MealPlanCreateDialog`, `MealPlanViewDialog`. B3 full-screen editor rewrite (numpad qty, sticky combined search, flat group headers), desktop today-hero + week strip, TODAY sliver, staples bar. **In the `/review-plan` set** — run `/review-plan` on the chunk-3 plan before implementing (spec §5 step 0). Enumerate one test per locked edit-decision (spec §4: validation/Done-disabled, dirty-cancel confirm, recipe `[× n]` chip, skip-toggle clear-confirm, empty states, remove-group confirm, flat group structure) + staples expand/collapse + numpad. **`review-code --base redesign-chunk-02`.** Now that `SectionThemeProvider` is wired centrally (see carryover below), chunk-3 meal-plan content just consumes `palette.primary` (= plans blue) — no per-layout theme wiring needed. When `fix/83` merges to main, back-merge main → branch first (clean — branch's middleware/auth match main).

**Chunk 2 — Nav chrome: DONE (2026-05-28), tagged `redesign-chunk-02` (commit `607f5b9`).** New dark `src/components/nav/*` (`TopNav`, `BottomNav`, `AppIcon`, `NavAvatar`, `AvatarMenu`, `SectionThemeProvider`) + shared `src/lib/nav-sections.ts` (`getSectionForPath` + `NAV_SECTIONS`) + `src/lib/hooks/use-active-section.ts`. `AuthenticatedLayout` rewired (renders chrome, wraps children in `SectionThemeProvider`, owns the unapproved redirect); old `Header.tsx` + `BottomNav.tsx` + old `BottomNav.test.tsx` removed. `npm run check` green (138 files / 1342 tests). `review-code` (base `redesign-chunk-01`, see base-note carryover) clean after 1 auto-fix (admin-bypass test). Verified: **desktop live via Claude-in-Chrome** (sections, active colors blue/orange/green, avatar menu no-Settings/no-Pantry/admin Manage-users, `SectionThemeProvider` accents); **mobile `BottomNav` + bottom sheet via unit tests** (couldn't force a phone viewport in Chrome — `resize_window` didn't reflow the screenshot). Manual plan: PR #89 `chunk-02-nav` comment; manifest `claude-design-redesign.chunk-02-nav.json`.

**Chunk 1 — Foundation: DONE (2026-05-28), tagged `redesign-chunk-01`.** Verified locally by the user (dark theme, Bricolage/Outfit + tabular nums, icon webfont, Settings placeholder, all surfaces dark, recipes open). `npm run check` green (131 files / 1326 tests). `/review-code --base redesign-chunk-00` clean. Draft PR #89 open. This session also: tabular-nums app-wide via CssBaseline (`5601b00`), manual-test seed-block id-shape fixes (`7888852`), **interim approval gate REVERTED** (`3004858`, now #83's — see note below), Vercel beta infra fully wired. Issues filed: **#88** (middleware dotted-path bypass, deferred), **#91** (route `createFromHexString` guards), **#92** (manual-test clean/orphan robustness), **#93** (worktree shared-DB decision). #83 left OPEN (owned by `fix/83`; can't close from this long-lived branch).

**Vercel beta deployment — DONE.** `beta.weekly-eats.zamilyfam.com` added to the `weekly-eats` Vercel project and bound to branch `claude-design-redesign` (gitBranch); DNS CNAME → `ec2bda90c0efdc89.vercel-dns-017.com` live in Squarespace-managed Google Cloud DNS (`misconfigured:false`); OAuth redirect URI `…/api/auth/callback/google` added to the **prd** client. Branch-scoped env on the project: `NEXTAUTH_URL=https://beta…`, and overrides so beta mirrors prod — `MONGODB_URI`→prod DB (`weekly-eats-prd`), `GOOGLE_CLIENT_ID`/`SECRET`→prd client (mtua). `ABLY_API_KEY`/`NEXTAUTH_SECRET` inherited from Preview. **Decision: beta runs on the PROD DB + PROD OAuth client** (per spec §1 — real dogfooding; honor the destructive-path discipline). Squarespace has no DNS API/CLI; OAuth Web-client URIs are Console-only — both were manual one-time human steps.

Per-chunk `review-code` uses `--base <prev-tag>` (now built into the skill). Chunk 3's base = `redesign-chunk-02`.

## Decisions & carryovers

Dated entries for things decided mid-flight that affect later chunks but aren't worth a spec rewrite.

- **2026-05-28 — A11y scope deferred (from `/review-plan`).** Consciously skipped for this single-family app, do not re-litigate: dark-mode muted-text contrast retune (`text.muted` #5b6170 / `text.past` #7b818f are sub-AA on dark surfaces by deliberate design choice), broad keyboard/focus rework for new surfaces, `@dnd-kit` `KeyboardSensor` for shopping reorder, mobile nav `aria-current`. **Kept:** accessible names on icon-only buttons (cheap, enforced by review-code each chunk).
- **2026-05-28 — Beta approval gating → owned by #83, NOT the redesign branch.** Chunk 1 originally added an interim server-side `token.isApproved` gate in `middleware.ts`, but it was **reverted** (`3004858`): prod and beta share the same prod DB + the same client-only gating, so a beta-only server gate gave no incremental protection while duplicating #83's work (back-merge conflict risk). The real fix lands via #83 → main → back-merge. **Kept** the client-gate-mount test (`AuthenticatedLayout.test.tsx`) — that's genuinely redesign-scoped (locks the mount so Chunk 2's `AuthenticatedLayout` rewrite can't silently drop the client gate). See the #83 note below.
- **2026-05-28 — Golden-master targets corrected (Chunk 0).** Only genuinely pure transforms get golden-master fixtures; `meal-plan-utils` overlap/next-date is the real target. The three sharing/shopping `*-utils` are async fetch wrappers → MSW path coverage instead. See spec §6 0c.
- **2026-05-28 — Chunk 0 closed.** Baseline green (`npm install` + `npm run check`: 1306 tests / 125 files). `tsconfig.json` now excludes `docs/` so the vendored design bundle isn't type-checked by `next build`. audit-debt sweep saved to `docs/debt-audit-2026-05-28.md` (58 findings). **Fixed in 0c** (behavior-preserving): Critical Ably token leak (now session-gated + capability scoped to the user's stores), 3× `ObjectId.isValid` guards, 2× mass-assignment allowlists (recipes/[id] PUT, user/settings POST), + data-layer test backfill (utils golden-master/MSW, 14 sharing/store route tests). **Backlog filed as issues #76–#86** (npm advisories, sharing-utils dup, a11y, monolith→Chunk 5, server-side approval→Chunk 1). `redesign-chunk-00` tag = the green base for Chunk 1's review-code override.
- **2026-05-28 — `next` high-severity npm advisory open (issue #76).** Dependency bumps were deliberately deferred in Chunk 0 (§0d triages, doesn't fix). Worth revisiting before the public beta deploy in Chunk 1.
- **2026-05-28 — Chunk 1 deviations (learned during impl, affect later chunks):**
  - **Material Symbols loads via a stylesheet `<link>`, NOT `next/font/google`.** That face isn't in next/font's registry for Next 15.5.9 (verified). `layout.tsx` injects the Google Fonts `<link>` (with `display=block` + a scoped `eslint-disable` for `no-page-custom-font`/`google-font-display` — both legit for an icon font), exposing the family via `--font-icons` in `globals.css`. `Icon.tsx` consumes `var(--font-icons)`. **Chunk 11 cleanup:** when removing `@mui/icons-material`, the Material Symbols `<link>` stays.
  - **`Icon` `sx` is a real `SxProps<Theme>`** (via a module-scope `styled('span')`, no per-render `Box`). Dynamic per-instance values (`fontFamily`/`fontSize`/`color`/`fontVariationSettings`) are inline `style`; `sx` is the caller-override hatch. Chose real SxProps over renaming to `style` because dev is fully agentic and agents pattern-match the `sx` convention (#89 review call). Decorative-by-default (`aria-hidden`); pass `aria-label` to make it `role="img"`.
  - **MUI v7 rejects custom typography variant names as `MuiTypography.styleOverrides` slots** (`displayXl` etc. → TS2353). That belt-and-suspenders override block was dropped; the typography _variants_ themselves carry `fontVariantNumeric: 'tabular-nums'`, so numeric tabular rendering is preserved. Don't re-add a `MuiTypography` slot override for custom variants.
  - **Signed-out landing page (`src/app/page.tsx`) still uses hardcoded LIGHT colors** — only the `lightTheme`→`darkTheme` import was swapped to compile. It will look light/mismatched until **Chunk 10** redesigns it. Expected interim wart; don't "fix" piecemeal.
  - **#88 filed** (middleware `pathname.includes('.')` defense-in-depth bypass) — deferred from Chunk 1; not an ownership hole (per-route `getServerSession` backstops). Revisit with route-level approval enforcement.
- **2026-05-28 — #83 (server-side approval) is owned by the parallel `fix/83-unapproved-users-access-issue` branch (CORRECTION of an earlier note).** Earlier this session the ledger said the parallel fix "never materialized" and Chunk 1 should own it — that was wrong; the parallel effort exists at worktree `/Users/zwrose/.loupe/workspaces/fix-83-unapproved-users-access-issue-038c8efb/weekly-eats` and is **further along + more complete** than Chunk 1's interim gate: middleware gate **+ `requireApprovedSession()` helper (`user-utils.ts`) + per-route data enforcement** (meal-plans routes) + auth `update`-trigger refresh + tests/fixtures. Chunk 1's interim middleware-only gate was **reverted** (`3004858`) to avoid duplication and a guaranteed back-merge conflict. **Plan:** #83 merges to main (closes #83 there — NOT from this branch; it can't merge until the long-lived redesign branch merges, which is far off, so #83 stays open meanwhile), then the normal `main → claude-design-redesign` back-merge brings the canonical fix here. That back-merge should be **clean** now (this branch's `middleware.ts`/`auth.ts`/`auth.test.ts` are restored to origin/main state). #83's route-level enforcement also largely neutralizes **#88** (the middleware dotted-path bypass) since data routes check approval directly. **Kept** `AuthenticatedLayout.test.tsx` (client-gate-mount lock) — complements #83's server-side tests.
- **2026-05-28 — Chunk 2 deviations & carryovers (affect later chunks):**
  - **`SectionThemeProvider` wired CENTRALLY in `AuthenticatedLayout`, not per route-layout.** It calls `useActiveSection()` internally and wraps `{children}`; every authenticated surface gets its section accent automatically. This deviates from spec §2's per-route-`layout.tsx` sketch and §4's "wired in chunks 3–6" note — same effect, DRYer, and sidesteps the §2 client-component-as-layout/`metadata` nesting constraint entirely. **Implication for chunks 3–6: do NOT re-wire `SectionThemeProvider` per surface — just consume `palette.primary`** (it already resolves to the section accent; `accentUtility` on system pages). Verified live: Create-Meal-Plan=blue, Add-Recipe=orange, Add-Store=green.
  - **`NavAvatar` shows the GOOGLE PROFILE PHOTO, initials as fallback** (user override of spec §3's "initials avatar", `99e201c`, landed just AFTER the `redesign-chunk-02` tag at `607f5b9` — so it rides into **chunk-3's review base** `redesign-chunk-02...HEAD`; that's fine). It **reuses `src/components/CachedAvatar.tsx`** (caching proxy + 429/error fallback) so `CachedAvatar` is **NO LONGER orphaned** — drop the earlier "delete in Chunk 11" note. Verified live (photo renders in the pill).
  - **Approval redirect moved `Header` → `AuthenticatedLayout`.** The unapproved-non-admin → `/pending-approval` redirect + nav-hide now lives in `AuthenticatedLayout` (one place, was scattered across `Header`+`BottomNav`). `useApprovalStatus` mount preserved (load-bearing test still green). Admin-bypass (`isApproved:false && isAdmin:true` → no redirect, nav stays) is now tested in `AuthenticatedLayout.test.tsx` (added by review-code auto-fix; the only prior coverage was in the deleted old `BottomNav.test.tsx`).
  - **Review base was `a534d62`, NOT the `redesign-chunk-01` tag.** A `Merge branch 'main'` commit (`a534d62`, pulling main's #90 review-suite changes + vendored design-bundle files) landed between the chunk-01 tag and chunk-2 work, polluting the tag-based diff. Reviewed `a534d62...HEAD` to isolate nav work. **Chunk 3 base = `redesign-chunk-02` tag** should be clean (no intervening main merge yet) — but check for a back-merge before trusting the tag diff; re-base on the pre-chunk commit if one appears.
  - **Untracked vendored bundle files.** `docs/design/weekly-eats-redesign/project/{src,public}/` were untracked at session start and got swept into a nav commit by a `git add -A`; amended back out, so they remain **untracked**. They're design-handoff reference (tsconfig excludes `docs/`). The user may want to commit the full bundle in its own `chore:` commit later — not redesign-chunk scope.
- **2026-05-28 — Chunk 0 hardening shipped to `main` separately (PR #87).** Extracted the 5 hardening commits onto `chunk-0-hardening` (off `main`), merged as #87 (squash), then back-merged `main` into this branch (merge commit `8fddab2`, clean no-op since the content was identical). The redesign-planning docs (spec, bundle, ledger) stayed on this branch only. Branch deleted local+remote. **Implication:** server-side approval (#83) is now being handled on its own branch in parallel — don't duplicate it in Chunk 1.
