# Design Redesign — Progress Ledger

Living dashboard for the dark-first redesign migration. This is the **compaction resume-anchor**: a freshly-compacted context reads the spec + this file to know where things stand. Git tags are the objective truth; this is the readable narrative.

- **Spec (roadmap):** `docs/superpowers/specs/2026-05-28-design-redesign-migration-design.md`
- **Design bundle (reference):** `docs/design/weekly-eats-redesign/`
- **Branch:** `claude-design-redesign` · **Beta:** `https://beta.weekly-eats.zamilyfam.com` (infra set up in Chunk 1 — Vercel domain bound to branch, DNS live, OAuth + env wired)
- **Draft PR:** [#89](https://github.com/zwrose/weekly-eats/pull/89) (long-lived, opened Chunk 1)
- **Audit backlog:** `docs/debt-audit-2026-05-28.md` _(created in Chunk 0)_

## Chunk status

| #   | Chunk                        | Status      | Tag               | Plan doc                                                                 | PR test comment | Done       |
| --- | ---------------------------- | ----------- | ----------------- | ------------------------------------------------------------------------ | --------------- | ---------- |
| 0   | Test baseline + hardening    | done        | redesign-chunk-00 | §6 of spec                                                               | n/a (no UI)     | 2026-05-28 |
| 1   | Foundation                   | done        | redesign-chunk-01 | redesign-chunk-01-foundation-plan.md                                     | PR #89 comment  | 2026-05-28 |
| 2   | Nav chrome                   | done        | redesign-chunk-02 | redesign-chunk-02-nav-plan.md                                            | PR #89 comment  | 2026-05-28 |
| 3   | Meal Plans                   | done        | redesign-chunk-03 | redesign-chunk-03-meal-plans-plan.md                                     | PR #89 comment  | 2026-05-29 |
| 4   | Recipes                      | done        | redesign-chunk-04 | redesign-chunk-04-recipes-plan.md _(review-plan)_                        | PR #89 comment  | 2026-05-30 |
| 5   | Shopping Lists               | in-progress | —                 | redesign-chunk-05-shopping-lists-plan.md _(gate #1 ✅ + review-plan ✅)_ | —               | —          |
| 6   | Pantry                       | pending     | —                 | —                                                                        | —               | —          |
| 7   | Food Items                   | pending     | —                 | —                                                                        | —               | —          |
| 8   | User Mgmt & Pending Approval | pending     | —                 | —                                                                        | —               | —          |
| 9   | Settings (placeholder)       | pending     | —                 | —                                                                        | —               | —          |
| 10  | Marketing / home             | pending     | —                 | —                                                                        | —               | —          |
| 11  | Cleanup                      | pending     | —                 | —                                                                        | —               | —          |

Status values: `pending` → `in-progress` → `done`. Per-chunk plans live at `docs/superpowers/plans/redesign-chunk-NN-<surface>-plan.md`.

## Next up

**Chunk 3 — Meal Plans: DONE (2026-05-29), tagged `redesign-chunk-03` (commit `b9fcf55`).** Built via subagent-driven-development (16 TDD tasks) then heavily refined during the user's hands-on testing: the **Template surface** as a pushed route `/meal-plans/template` (`TemplateSettings`), the **Sharing surface** as a designed sheet/dialog (`ShareMealPlansDialog`), staples-group expansion, recipe-emoji read-side join, pending-invite read-side surfacing (+ status pill), discard-changes guard, mobile padding/spacing fixes, and **PWA branding** (dark manifest/viewport/color-scheme + regenerated icons from the AppIcon logomark via `scripts/generate-app-icons.mjs`). Final `/review-code --base redesign-chunk-02` (15.8k-line diff, 4 specialists) came back **READY FOR PR** — 0 Critical/Important; 1 auto-fix round resolved 2 Minor + 1 Nit (dynamic ShareMealPlansDialog import, recipe-emoji fetch → `fetchRecipeEmojiMap` in recipe-utils, date-utils alias), 1 Nit skipped (util placement). Pushed. **Back-merged `main` (commit `b9be176`)** — brought #83 server-side approval + dep upgrades (TS6, Node24, zod4, React 19.2.6, mongodb 6.21, **@mui/material 7.3**). Merge was clean except the shared-users route test (resolved: #83 approval scaffolding + chunk-3 pending/status). **MUI 7.3 fallout fixed:** Switch input now `role="switch"` and `inputProps` no longer forwards `aria-label` → meal-toggle Switches moved to `slotProps.input`; tests query role `switch`. `npm run check` GREEN (lint + 1534 tests + build).

**Chunk 4 — Recipes: DONE (2026-05-30), tagged `redesign-chunk-04` (commit `ecba86d`).** Built via `superpowers:subagent-driven-development` (the 18-task `redesign-chunk-04-recipes-plan.md`), then a hands-on user review + a **rigorous artboard-fidelity alignment pass**. Shape: view = real route `/recipes/[id]` (`RecipeDetail`); edit+create = full-page in-page takeovers via shared `RecipeEditor` (not routes); tap-a-recipe-to-open re-added in `EditorItemRow` (in-PWA `router.push('/recipes/<id>')`); old `?viewRecipe=` redirects to the route. All 6 recipe surfaces (View/Edit/List/Filter/Sharing/Emoji) aligned to the artboards at both breakpoints; unit pluralization; seed-fidelity fixes. **Also this session:** a post-hoc **artboard audit of chunks 1–3** (`docs/design/weekly-eats-redesign/AS-BUILT-VS-ARTBOARDS-AUDIT.md` + disposition log) → closed material gaps (theme `body.md` 500 + canonical button radius 10; nav AvatarMenu identity header/colored icons/sheet chrome; MP index PlanRow rebuild + section rules + ghost-icon header; template borderless cards + shared `ui/PillSwitch`; sharing dialog; mobile meal-editor as a tall sheet; CombinedSearch results integrated into one box). Kept-as-built deviations recorded (red dot, history accordion, expanded staples, "Account" label). **Bug fixes:** staples names now enriched in `/api/meal-plans/[id]` (was placeholder); `EditorGroupSection` group title `??`→`||`. `manual-testing chunk-04-recipes` posted to PR #89. **Two `main` back-merges:** `d5e9c2f` (vitest 3→4 / vite 8 / jsdom 29 + review-skills #145) and `fdfd62f` (**NextAuth v4→v5** #142/#146 + **ably v2** #141). **`npm run check` GREEN** (lint + 1459 tests / 167 files + build). ⚠️ The NextAuth v5 cookie/JWT change may log out existing browser/beta sessions — re-login if a page 401s. **Keep** old `src/components/EmojiPicker.tsx` — chunk 5 (shopping) uses it.

---

## NEXT UP — Chunk 5: Shopping Lists (in-progress 2026-05-30; cold-start)

**State at handoff:** branch synced — HEAD = `dc620ba` (chunk-5 plan, converged), 0 behind `origin/main`, plan commits unpushed (long-lived branch — push happens at the per-chunk push step). Dev server may be running on `:3235` (restart with `npm run dev` if not; `npm run clean` first if Turbopack errors after a build). Beta runs on PROD DB + NextAuth v5.

**✅ Step 0 DONE (2026-05-30):** `docs/superpowers/plans/redesign-chunk-05-shopping-lists-plan.md` written (full §3 exact-value spec at both breakpoints), **Artboard gate #1 signed off** (C1=in-page master-detail / C2=default KEEP / C3=full presence pill w/ derived avatars / C6=both finish-confirms), and **`/review-plan` converged → PLAN READY** over 2 rounds (see plan §7). Plan = 11 tasks (1 tokens, 2 PresenceAvatar, 3 KeepSkipToggle, 3A renderWithTheme, 3B StoreListView, 4 two-pane shell ⭐, 5 item rows, 6 finish bar+confirm, 7 presence pill, 8 pantry KEEP/SKIP, 9 dialogs+flat emoji, 10 page slim-down, 11 validate).

**✅ Step 1 IMPLEMENTATION DONE (2026-05-30)** via `superpowers:subagent-driven-development` — all 11 tasks (each with two-stage spec+quality review), 21 commits `f7e03db…c3d2aa1`. **`npm run check` GREEN** (lint --max-warnings=0 + full test suite + build; cleared `.next` for the documented stale-cache build flake). Shopping suite 100 tests across 23 files. Built: on-accent/on-danger tokens; `PresenceAvatar`+derived color/initials; `KeepSkipToggle`; `renderWithTheme` test util (shop-accent-bound); `StoreListView` index extraction; **two-pane working view (`ShoppingListView`+`StoreSidebar`, no modal)** w/ `useShoppingSync enabled: selectedStore!==null`; sortable `ShoppingItemRow` (native checkbox)+`AddItemRow`; solid `FinishShopBar`+`FinishShopConfirm` (desktop dialog/mobile sheet); unified `PresencePill` (6 states, derived avatars); `PantryCheckDialog` KEEP/SKIP (default KEEP); **shared `ui/EmojiPicker`** (recipes+shopping consume it; legacy default-export deleted, `FOOD_EMOJIS`→`food-emojis.ts`); extracted+restyled StoreEditor/ImportFromPlans/UnitConflict/ShareStore/ItemEditor/StoreHistory dialogs; `StoreActionsMenu`; page.tsx 2885→~1721 lines (orchestration-only), raw hex purged. **No API/write-logic change** (verified by security + spec reviews). Known kept-as-built fidelity gaps to record in Gate #2: index `lastShop`/shared-member-names/`inviterName` (data not surfaced without API change); pantry per-row qty-adjust dropped (binary KEEP/SKIP per artboard).

**✅ Step 2 — Gate #2 artboard-fidelity audit DONE (2026-05-30):** `redesign-chunk-05-shopping-lists-artboard-audit.md` (value-by-value + live dual-breakpoint via chrome-devtools-mcp on `:3235`). Found 5 Material + many Minor gaps; closed in 3 batches (`fa84873`/`73ea24a`/`6e05a17`) — incl. the **item-row centering bug**, group-container boxes, finish-bar anchoring, index search double-box, dialog widths, pantry/presence container styling. Verified live at both breakpoints. Kept-as-built recorded (in-page BottomNav per C1=A; index inline row-actions; no-data fields; pantry qty-adjust dropped).

**✅ Step 3 — `review-code --base redesign-chunk-04` DONE (2026-05-30): READY FOR PR.** Branch mode, chunk-5 delta (54 files, +5824/−1785). Round 1: **0 Critical / 0 Important** (security clean — no API/write-logic change confirmed; test clean — 3 required cases + page.test migration verified). 6 Minor/Nit: 2 mechanical Code Nits auto-fixed (`ce43bb1` — redundant `role=button`, duplicate StoreRow shared icon); 4 Architecture Minor/Nit skipped with POV (off-screen mobile sidebar required by the verbatim test; `DefaultItemRow` load-bearing for the component test; helper-dup/format-dup low value). **`npm run check` GREEN** (lint + **1509 tests** + build). **Pushed** `ce43bb1` (CI + beta rebuilding).

**▶ NEXT: Step 5 — `manual-testing chunk-05-shopping-lists`** (seed local dev DB + post checkbox test plan to PR #89) → **Step 7 the local verification gate** (user clicks through localhost — THE gate, not beta) → tag `redesign-chunk-05` (after gate passes) → ledger closeout → merge `main` (if moved) → compact. Dev server is up on `:3235` (authenticated session live).

**Cold-start checklist (reference):**

1. **Read:** this ledger + spec **§4/§5** + the converged plan `docs/superpowers/plans/redesign-chunk-05-shopping-lists-plan.md` (the artboard exact-value spec lives in plan §3, so re-reading the 1602-line `.jsx` is optional now).
2. ~~Step 0 — Plan + gate #1 + review-plan~~ **✅ DONE** (see above).
3. **Scope (spec §4, as locked in the plan):** in-page master-detail two-pane desktop (store sidebar + working list, **no modal**) + mobile pushed view, presence pill (full multi-user, derived avatars), **KEEP/SKIP toggle in pantry check** (default KEEP), solid finish-shop bar + confirm, flat shared emoji picker. **Visual-only on realtime/presence — NO API / write-logic change.** Required tests: store-pane↔working-list selection (Task 4), KEEP/SKIP filtering (Task 8), finish-shop bar (Task 6).
4. **Reuse:** old `src/components/EmojiPicker.tsx` is kept specifically for shopping — use/restyle it for the spec's "flat emoji picker." Chunk-3/4 primitives (`ConfirmDialog`, etc.) where applicable.
5. **Review base** = `redesign-chunk-04` tag (`ecba86d`) — clean (no `main` merge intervenes yet). If `main` moves before the chunk-5 review, re-base on the post-merge commit (precedent: chunks 2 & 4).
6. **Then the per-chunk loop (spec §5):** implement (logical commits, `npm run check` green) → **artboard gate #2** (`docs/superpowers/plans/redesign-chunk-05-shopping-lists-artboard-audit.md`, value-by-value + live dual-breakpoint audit, close gaps **before** review-code) → `review-code` auto-fix loop → `npm run check` → `manual-testing chunk-05-shopping-lists` → push → execute the checklist (the gate) → tag `redesign-chunk-05` → update this ledger → merge `main` → compact.

**Chunk 2 — Nav chrome: DONE (2026-05-28), tagged `redesign-chunk-02` (commit `607f5b9`).** New dark `src/components/nav/*` (`TopNav`, `BottomNav`, `AppIcon`, `NavAvatar`, `AvatarMenu`, `SectionThemeProvider`) + shared `src/lib/nav-sections.ts` (`getSectionForPath` + `NAV_SECTIONS`) + `src/lib/hooks/use-active-section.ts`. `AuthenticatedLayout` rewired (renders chrome, wraps children in `SectionThemeProvider`, owns the unapproved redirect); old `Header.tsx` + `BottomNav.tsx` + old `BottomNav.test.tsx` removed. `npm run check` green (138 files / 1342 tests). `review-code` (base `redesign-chunk-01`, see base-note carryover) clean after 1 auto-fix (admin-bypass test). Verified: **desktop live via Claude-in-Chrome** (sections, active colors blue/orange/green, avatar menu no-Settings/no-Pantry/admin Manage-users, `SectionThemeProvider` accents); **mobile `BottomNav` + bottom sheet via unit tests** (couldn't force a phone viewport in Chrome — `resize_window` didn't reflow the screenshot). Manual plan: PR #89 `chunk-02-nav` comment; manifest `claude-design-redesign.chunk-02-nav.json`.

**Chunk 1 — Foundation: DONE (2026-05-28), tagged `redesign-chunk-01`.** Verified locally by the user (dark theme, Bricolage/Outfit + tabular nums, icon webfont, Settings placeholder, all surfaces dark, recipes open). `npm run check` green (131 files / 1326 tests). `/review-code --base redesign-chunk-00` clean. Draft PR #89 open. This session also: tabular-nums app-wide via CssBaseline (`5601b00`), manual-test seed-block id-shape fixes (`7888852`), **interim approval gate REVERTED** (`3004858`, now #83's — see note below), Vercel beta infra fully wired. Issues filed: **#88** (middleware dotted-path bypass, deferred), **#91** (route `createFromHexString` guards), **#92** (manual-test clean/orphan robustness), **#93** (worktree shared-DB decision). #83 left OPEN (owned by `fix/83`; can't close from this long-lived branch).

**Vercel beta deployment — DONE.** `beta.weekly-eats.zamilyfam.com` added to the `weekly-eats` Vercel project and bound to branch `claude-design-redesign` (gitBranch); DNS CNAME → `ec2bda90c0efdc89.vercel-dns-017.com` live in Squarespace-managed Google Cloud DNS (`misconfigured:false`); OAuth redirect URI `…/api/auth/callback/google` added to the **prd** client. Branch-scoped env on the project: `NEXTAUTH_URL=https://beta…`, and overrides so beta mirrors prod — `MONGODB_URI`→prod DB (`weekly-eats-prd`), `GOOGLE_CLIENT_ID`/`SECRET`→prd client (mtua). `ABLY_API_KEY`/`NEXTAUTH_SECRET` inherited from Preview. **Decision: beta runs on the PROD DB + PROD OAuth client** (per spec §1 — real dogfooding; honor the destructive-path discipline). Squarespace has no DNS API/CLI; OAuth Web-client URIs are Console-only — both were manual one-time human steps.

Per-chunk `review-code` uses `--base <prev-tag>` (now built into the skill). Chunk 3's base = `redesign-chunk-02`.

## Decisions & carryovers

Dated entries for things decided mid-flight that affect later chunks but aren't worth a spec rewrite.

- **2026-05-30 — Artboard-compliance review is now a TWO-gate requirement for every remaining chunk (5–11).** Added to spec §5 (per-chunk loop steps 0 & 2 + Definition of Done). This exists because chunks 3/4 drifted from the artboards and needed a post-hoc audit + alignment pass (`docs/design/weekly-eats-redesign/AS-BUILT-VS-ARTBOARDS-AUDIT.md`). The two gates, per chunk with artboards:
  1. **Plan-time (raise concerns before code):** the chunk plan MUST carry an **Artboard compliance** section — exact-value spec per screen at both breakpoints **+** an **Artboard concerns** subsection flagging anything unrealistic / costly / likely-bad-idea with an `honor/adapt/drop` recommendation for the **user to rule on before implementing**. `/review-plan` reviews it.
  2. **Post-implementation (catch drift before review-code):** value-by-value **+** live dual-breakpoint (desktop + ~390–430px, seeded data, chrome-devtools-mcp) audit → `…-chunk-NN-<surface>-artboard-audit.md` with a **disposition log** (close vs keep-as-built, recorded once). Close the gaps **before** `review-code` so fixes are in the reviewed diff.
     Conditional: surfaces with no/thin artboards (9 Settings placeholder, parts of 10/11) note "no artboards — N/A" and skip both. The generic `/review-plan` and `/review-code` skills were intentionally NOT modified (kept project-agnostic); the process lives in the spec loop + this ledger.
- **2026-05-30 — Back-merged `main` again (merge `fdfd62f`) — NextAuth v5 + ably v2 + pushed.** Brought main's 2 commits: **#142/#146 Auth.js v4→v5 redirect proxy** (rewrites `auth.ts`/`middleware.ts`, adds `auth.config.ts`, migrates the auth-mock pattern `getServerSession`→`auth()` across ~50 route tests) and **#141 ably→v2**. Despite the framework migration, only **4 conflicts**: 3 modify/delete on the old nav files (`BottomNav.tsx`/`Header.tsx`/`BottomNav.test.tsx` — chunk-2 deleted them; **kept deleted**) + 1 content conflict in `meal-plan-sharing/shared-users/route.test.ts` (combined main's v5 `auth` mock with the branch's pending/status assertions). All ~50 shared route files auto-merged to main's v5 versions (branch never touched them); the new redesign nav uses the _client_ API (`next-auth/react`), unchanged in v5 — so **no v4 server-auth remnants repo-wide** post-merge. `npm install` (next-auth `5.0.0-beta.31`, ably `^2`). **`npm run check` GREEN: lint + 1459 tests / 167 files + build.** ⚠️ The v4→v5 cookie/JWT format change may log out existing browser sessions — re-login if the live session 401s. Branch 0 behind `origin/main`, pushed.
- **2026-05-30 — Back-merged `main` (merge `d5e9c2f`) + pushed.** Brought main's 2 commits into the branch: **#139 test-toolchain bump (vitest 3→4, vite→8, jsdom→29)** and #145 review-skills SKILL/docs. One trivial conflict — `RecipeStarRating.test.tsx` modify/delete (chunk-4 deleted `RecipeStarRating`; main patched its test for vitest 4) — resolved by keeping the deletion. `package.json`/lock/`vitest.config.ts` auto-merged (branch hadn't touched them since merge-base), so vitest 4 came in clean. **`npm run check` GREEN under the new toolchain: lint + 1450 tests / 167 files + build** — zero test changes needed (main's config migration covered v4). Branch was also 22 commits behind origin; pushed a pre-merge checkpoint (`a16012b`, incl. the as-built-vs-artboards audit + chunk-4 deviations doc + manual manifests) then the merge. Branch now 0 behind `origin/main`, fully pushed.
- **2026-05-30 — Chunk-3/4 artboard-fidelity pass (post-recipes).** After a rigorous as-built-vs-artboards audit (`docs/design/weekly-eats-redesign/AS-BUILT-VS-ARTBOARDS-AUDIT.md`), closed the material gaps on the earlier built surfaces: theme (`body.md` 500 + canonical button radius 10), nav AvatarMenu (identity header + colored icons + mobile sheet chrome), meal-plans index PlanRow rebuild + section-label rules + ghost-icon header, template borderless cards + pill toggle, sharing dialog alignment, and the mobile meal editor dressed as a tall bottom sheet. **Kept-as-built (spec to reconcile, not code):** red notification dot, history year→month accordion, expanded staples, "Account" nav label, live counts, per-owner shared grouping. See the audit doc's disposition log.
- **2026-05-28 — A11y scope deferred (from `/review-plan`).** Consciously skipped for this single-family app, do not re-litigate: dark-mode muted-text contrast retune (`text.muted` #5b6170 / `text.past` #7b818f are sub-AA on dark surfaces by deliberate design choice), broad keyboard/focus rework for new surfaces, `@dnd-kit` `KeyboardSensor` for shopping reorder, mobile nav `aria-current`. **Kept:** accessible names on icon-only buttons (cheap, enforced by review-code each chunk).
- **2026-05-28 — Beta approval gating → owned by #83, NOT the redesign branch.** Chunk 1 originally added an interim server-side `token.isApproved` gate in `middleware.ts`, but it was **reverted** (`3004858`): prod and beta share the same prod DB + the same client-only gating, so a beta-only server gate gave no incremental protection while duplicating #83's work (back-merge conflict risk). The real fix lands via #83 → main → back-merge. **Kept** the client-gate-mount test (`AuthenticatedLayout.test.tsx`) — that's genuinely redesign-scoped (locks the mount so Chunk 2's `AuthenticatedLayout` rewrite can't silently drop the client gate). See the #83 note below.
- **2026-05-28 — Golden-master targets corrected (Chunk 0).** Only genuinely pure transforms get golden-master fixtures; `meal-plan-utils` overlap/next-date is the real target. The three sharing/shopping `*-utils` are async fetch wrappers → MSW path coverage instead. See spec §6 0c.
- **2026-05-28 — Chunk 0 closed.** Baseline green (`npm install` + `npm run check`: 1306 tests / 125 files). `tsconfig.json` now excludes `docs/` so the vendored design bundle isn't type-checked by `next build`. audit-debt sweep saved to `docs/debt-audit-2026-05-28.md` (58 findings). **Fixed in 0c** (behavior-preserving): Critical Ably token leak (now session-gated + capability scoped to the user's stores), 3× `ObjectId.isValid` guards, 2× mass-assignment allowlists (recipes/[id] PUT, user/settings POST), + data-layer test backfill (utils golden-master/MSW, 14 sharing/store route tests). **Backlog filed as issues #76–#86** (npm advisories, sharing-utils dup, a11y, monolith→Chunk 5, server-side approval→Chunk 1). `redesign-chunk-00` tag = the green base for Chunk 1's review-code override.
- **2026-05-28 — `next` high-severity npm advisory open (issue #76).** Dependency bumps were deliberately deferred in Chunk 0 (§0d triages, doesn't fix). Worth revisiting before the public beta deploy in Chunk 1.
- **2026-05-28 — Chunk 1 deviations (learned during impl, affect later chunks):**
  - **Material Symbols loads via a stylesheet `<link>`, NOT `next/font/google`.** That face isn't in next/font's registry for Next 15.5.9 (verified). `layout.tsx` injects the Google Fonts `<link>` (with `display=block` + a scoped `eslint-disable` for `no-page-custom-font`/`google-font-display` — both legit for an icon font), exposing the family via `--font-icons` in `globals.css`. `Icon.tsx` consumes `var(--font-icons)`. **Chunk 11 cleanup:** when removing `@mui/icons-material`, the Material Symbols `<link>` stays.
  - **`Icon` `sx` is a real `SxProps<Theme>`** (via a module-scope `styled('span')`, no per-render `Box`). Dynamic per-instance values (`fontFamily`/`fontSize`/`color`/`fontVariationSettings`) are inline `style`; `sx` is the caller-override hatch. Chose real SxProps over renaming to `style` because dev is fully agentic and agents pattern-match the `sx` convention (#89 review call). Decorative-by-default (`aria-hidden`); pass `aria-label` to make it `role="img"`.
  - **MUI v7 rejects custom typography variant names as `MuiTypography.styleOverrides` slots** (`displayXl` etc. → TS2353). That belt-and-suspenders override block was dropped; the typography _variants_ themselves carry `fontVariantNumeric: 'tabular-nums'`, so numeric tabular rendering is preserved. Don't re-add a `MuiTypography` slot override for custom variants.
  - **Signed-out landing page (`src/app/page.tsx`) still uses hardcoded LIGHT colors** — only the `lightTheme`→`darkTheme` import was swapped to compile. It will look light/mismatched until **Chunk 10** redesigns it. Expected interim wart; don't "fix" piecemeal.
  - **#88 filed** (middleware `pathname.includes('.')` defense-in-depth bypass) — deferred from Chunk 1; not an ownership hole (per-route `getServerSession` backstops). Revisit with route-level approval enforcement.
- **2026-05-28 — #83 (server-side approval) is owned by the parallel `fix/83-unapproved-users-access-issue` branch (CORRECTION of an earlier note).** Earlier this session the ledger said the parallel fix "never materialized" and Chunk 1 should own it — that was wrong; the parallel effort exists at worktree `/Users/zwrose/.loupe/workspaces/fix-83-unapproved-users-access-issue-038c8efb/weekly-eats` and is **further along + more complete** than Chunk 1's interim gate: middleware gate **+ `requireApprovedSession()` helper (`user-utils.ts`) + per-route data enforcement** (meal-plans routes) + auth `update`-trigger refresh + tests/fixtures. Chunk 1's interim middleware-only gate was **reverted** (`3004858`) to avoid duplication and a guaranteed back-merge conflict. **Plan:** #83 merges to main (closes #83 there — NOT from this branch; it can't merge until the long-lived redesign branch merges, which is far off, so #83 stays open meanwhile), then the normal `main → claude-design-redesign` back-merge brings the canonical fix here. That back-merge should be **clean** now (this branch's `middleware.ts`/`auth.ts`/`auth.test.ts` are restored to origin/main state). #83's route-level enforcement also largely neutralizes **#88** (the middleware dotted-path bypass) since data routes check approval directly. **Kept** `AuthenticatedLayout.test.tsx` (client-gate-mount lock) — complements #83's server-side tests.
- **2026-05-28 — Chunk 3 plan deviation: meal-plan detail is a REAL ROUTE, not a dialog (user decision).** Spec §4 named recipes as "the one real routing change"; the user chose `/meal-plans/[id]` (with `‹ Plans` back button) here too — stacking the B3 meal editor on a plan-detail dialog felt bad, and a route matches the recipes detail pattern. So Chunk 3 adds `src/app/meal-plans/[id]/{page,loading,error}.tsx` + `src/components/meal-plans/PlanDetail.tsx`; `MealPlanViewDialog.tsx` is **deleted**; the index navigates to the route and **redirects old `?viewMealPlan=` deep-links**. **Implication for Chunk 4:** recipes' full-page route is now the _second_ such route, not the first — the UI `[id]` pattern (server `page.tsx` awaits async params → wraps a client detail component in `AuthenticatedLayout`) is established here; reuse it. Also: the index "Past · last 6 weeks" section is built (existing `GET /api/meal-plans` with a date range — no new endpoint); only the standalone History _route_ is deliberately not built (existing `MealPlanBrowser` accordion covers it).
- **2026-05-28 — Chunk 2 deviations & carryovers (affect later chunks):**
  - **`SectionThemeProvider` wired CENTRALLY in `AuthenticatedLayout`, not per route-layout.** It calls `useActiveSection()` internally and wraps `{children}`; every authenticated surface gets its section accent automatically. This deviates from spec §2's per-route-`layout.tsx` sketch and §4's "wired in chunks 3–6" note — same effect, DRYer, and sidesteps the §2 client-component-as-layout/`metadata` nesting constraint entirely. **Implication for chunks 3–6: do NOT re-wire `SectionThemeProvider` per surface — just consume `palette.primary`** (it already resolves to the section accent; `accentUtility` on system pages). Verified live: Create-Meal-Plan=blue, Add-Recipe=orange, Add-Store=green.
  - **`NavAvatar` shows the GOOGLE PROFILE PHOTO, initials as fallback** (user override of spec §3's "initials avatar", `99e201c`, landed just AFTER the `redesign-chunk-02` tag at `607f5b9` — so it rides into **chunk-3's review base** `redesign-chunk-02...HEAD`; that's fine). It **reuses `src/components/CachedAvatar.tsx`** (caching proxy + 429/error fallback) so `CachedAvatar` is **NO LONGER orphaned** — drop the earlier "delete in Chunk 11" note. Verified live (photo renders in the pill).
  - **Approval redirect moved `Header` → `AuthenticatedLayout`.** The unapproved-non-admin → `/pending-approval` redirect + nav-hide now lives in `AuthenticatedLayout` (one place, was scattered across `Header`+`BottomNav`). `useApprovalStatus` mount preserved (load-bearing test still green). Admin-bypass (`isApproved:false && isAdmin:true` → no redirect, nav stays) is now tested in `AuthenticatedLayout.test.tsx` (added by review-code auto-fix; the only prior coverage was in the deleted old `BottomNav.test.tsx`).
  - **Review base was `a534d62`, NOT the `redesign-chunk-01` tag.** A `Merge branch 'main'` commit (`a534d62`, pulling main's #90 review-suite changes + vendored design-bundle files) landed between the chunk-01 tag and chunk-2 work, polluting the tag-based diff. Reviewed `a534d62...HEAD` to isolate nav work. **Chunk 3 base = `redesign-chunk-02` tag** should be clean (no intervening main merge yet) — but check for a back-merge before trusting the tag diff; re-base on the pre-chunk commit if one appears.
  - **Untracked vendored bundle files.** `docs/design/weekly-eats-redesign/project/{src,public}/` were untracked at session start and got swept into a nav commit by a `git add -A`; amended back out, so they remain **untracked**. They're design-handoff reference (tsconfig excludes `docs/`). The user may want to commit the full bundle in its own `chore:` commit later — not redesign-chunk scope.
- **2026-05-28 — Chunk 0 hardening shipped to `main` separately (PR #87).** Extracted the 5 hardening commits onto `chunk-0-hardening` (off `main`), merged as #87 (squash), then back-merged `main` into this branch (merge commit `8fddab2`, clean no-op since the content was identical). The redesign-planning docs (spec, bundle, ledger) stayed on this branch only. Branch deleted local+remote. **Implication:** server-side approval (#83) is now being handled on its own branch in parallel — don't duplicate it in Chunk 1.
- **2026-05-29 — Tap-a-recipe-to-open from meal plans DEFERRED to chunk 4 (user-approved).** Prod's deleted `MealPlanViewDialog` linked recipes to `/recipes?viewRecipe=true&viewRecipe_recipeId=<id>`; chunk 3 renders recipes as plain text so the affordance is currently absent. Re-add in chunk 4 against the new `/recipes/[id]` route. **Key requirement:** the tap must stay INSIDE the installed PWA — use `router.push`/`<Link>` client nav, never `window.open`/`target="_blank"` (and avoid a plain same-origin `<a href>`, which iOS standalone PWAs can kick out to Safari). Full note + test requirement recorded in the spec (§4 "Tap-a-recipe-to-open from meal plans" + chunk-4 test list).
- **2026-05-29 — Chunk 3 gap fixed during user review: Template + Sharing surfaces were under-delivered, now built to the artboards.** The original chunk-3 plan only gave template/sharing a one-line "restyle" mention, so the 16 TDD tasks shipped them as generic MUI dialogs (raw checkboxes, `Select`, `TextField`+`List`). The artboards (`artboards-mp-{desktop,mobile}.jsx`) specify full surfaces. Built post-hoc (still in chunk 3, pre-tag): **Template = a pushed route** `src/app/meal-plans/template/*` + `TemplateSettings.tsx` (`c672d27`) — day-chips, meal-color toggle rows, staples card; gear navigates there; inline template dialog + staples editor removed from `page.tsx`. **Sharing = `ShareMealPlansDialog.tsx`** (`230cb59`) — bottom sheet (mobile) / dialog (desktop), pending-invite cards + invite-by-email + shared-with rows. **Deviations:** (a) pending invitations moved INTO the sheet → the index "Invitations" banner was removed (dot badge still signals); (b) `SharedUser` has no accepted/pending field, so the artboard's status pill is omitted (no fabricated state / no new endpoint); (c) Template is a route (matches the mobile "pushed screen" artboard + the plan-detail route decision), not a dialog — user-approved. Same `updateMealPlanTemplate`/invite-respond-remove payloads — no write-logic change. 106 meal-plans tests green; both verified live at 390px + desktop via chrome-devtools-mcp.
